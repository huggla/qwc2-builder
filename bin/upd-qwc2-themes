#!/bin/sh
startdir=`pwd`
chuser="chpst -u ${USER}"
configure="cp -rf /qwc2conf/* /qwc2"
update="yarn run themesconfig --cache-folder /home/user/.cache"
cd /qwc2
if [ "`id -u`" -eq 0 ]
then
    $chuser $configure
    $chuser $update
else
    $configure
    $update
fi
cd $startdir
exit
