#!/bin/sh
startdir=`pwd`
install="yarn install"
configure="cp -rf /qwc2conf/* ./"
update="yarn run prod"
replace="cp -rf /usr/local/src/qwc2-demo-app/* /qwc2"
chuser="chpst -u ${USER}"
cd /usr/local/src/qwc2-demo-app
if ((EUID))
then
    $install
    $configure
    $update
    $replace
else
    $chuser $install
    $chuser $configure
    $chuser $update
    $chuser $replace
fi
cd $startdir
